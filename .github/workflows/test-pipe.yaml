# Yolo
on:
  workflow_dispatch:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master      
jobs:
  soSomething:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - run: echo "Hello World"
    - run: ls
    
